apiVersion: v1
kind: Service
metadata:
  name: fintrack-frontend-service
spec:
  selector:
    app: fintrack-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: nodePort
  